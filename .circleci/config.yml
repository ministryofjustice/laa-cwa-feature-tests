version: 2.1

orbs:
  ruby: circleci/ruby@2.0.1

jobs:
  test:
    docker:
      - image: 'cimg/ruby:2.7.6-browsers'
    steps:
      - checkout
      - run:
          name: Run tests
          command: bundle exec cucumber features/ 

workflows:
  version: 2
  build_and_test:
    jobs:
      - test